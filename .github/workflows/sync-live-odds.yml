name: sync-live-odds
# Free-source verification run.

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */4 * * *"
  push:
    branches: ["main", "feature/betting-edge-workspace"]
    paths:
      - ".github/workflows/sync-live-odds.yml"
      - "web/scripts/sync-live-odds.mjs"
      - "web/scripts/sync-free-oddsportal.mjs"

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SUPABASE_URL: https://nlmtcimkqymynsyflimv.supabase.co
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      THE_ODDS_API_KEY: ${{ secrets.THE_ODDS_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install free bookmaker collector
        working-directory: web
        run: npm install --no-save odds-portal-scraper@2.10.6
      - name: Install browser runtime
        working-directory: web
        run: npx playwright install --with-deps chromium
      - name: Refresh free fixture spine
        run: node web/scripts/sync-live-odds.mjs
      - name: Collect capped free bookmaker prices
        run: node web/scripts/sync-free-oddsportal.mjs
      - name: Process alerts and closing lines
        run: node web/scripts/sync-live-odds.mjs
